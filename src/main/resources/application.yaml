server:
  port: ${PORT:8080}

spring:
  application:
    name: taskflow-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

spring.jpa:
  show-sql: true
  properties:
    hibernate:
      format_sql: true
